[init]
    defaultBranch = main

[core]
    editor = code --wait
    excludesfile = ~/.gitignore_global

[pull]
    rebase = false

[push]
    autoSetupRemote = true

[alias]
    co = checkout
    br = branch
    ci = commit
    st = status
